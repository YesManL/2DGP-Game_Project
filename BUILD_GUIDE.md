# 🎮 Sand Raiders - 빌드 및 배포 가이드

## 📦 두 가지 빌드 방식

### 1️⃣ 단일 EXE 파일 (--onefile) ✨ 권장

**실행 방법:**
```bash
build_onefile.bat
```

**특징:**
- ✅ **배포 편리**: 하나의 EXE 파일만 전달
- ✅ **간단함**: 폴더 구조 신경 쓸 필요 없음
- ⚠️ **파일 크기**: 약 200~300MB (모든 리소스 포함)
- ⚠️ **실행 시간**: 첫 실행 시 1~3초 압축 해제 시간
- 📁 **출력**: `dist/SandRaiders.exe` (단일 파일)

**언제 사용?**
- 게임을 친구들에게 공유할 때
- 간단하게 배포하고 싶을 때
- 사용자가 기술적으로 익숙하지 않을 때

---

### 2️⃣ 폴더 방식 (--onedir)

**실행 방법:**
```bash
build_game.bat
```
또는
```bash
quick_build.bat
```

**특징:**
- ✅ **실행 빠름**: 즉시 실행 (압축 해제 없음)
- ✅ **개발 편리**: 디버깅 시 파일 확인 쉬움
- ⚠️ **배포 복잡**: 폴더 전체를 압축해서 전달해야 함
- ⚠️ **여러 파일**: _internal 폴더 + EXE 파일
- 📁 **출력**: `dist/SandRaiders/` 폴더

**언제 사용?**
- 개발 및 테스트 중일 때
- 게임이 자주 업데이트될 때
- Steam 같은 플랫폼에서 자동 설치할 때

---

## 🚀 빠른 시작

### 최초 설정 (한 번만)
```bash
pip install pyinstaller
```

### 단일 EXE 빌드 (배포용)
```bash
build_onefile.bat
```
➡️ `dist/SandRaiders.exe` 생성 (이 파일만 배포)

### 폴더 빌드 (개발/테스트용)
```bash
quick_build.bat
```
➡️ `dist/SandRaiders/` 폴더 생성 (폴더 전체 필요)

---

## 📋 상세 빌드 명령어

### 단일 EXE 빌드
```bash
pyinstaller --clean --noconfirm SandRaiders.spec
```

### 처음부터 새로 빌드
```bash
pyinstaller --clean --noconfirm SandRaiders.spec
```

---

## 📤 배포 가이드

### 단일 EXE 방식
1. `dist/SandRaiders.exe` 파일만 사용자에게 전달
2. 사용자는 다운로드 후 바로 더블 클릭 실행
3. 끝!

### 폴더 방식
1. `dist/SandRaiders/` 폴더 전체를 ZIP 압축
2. 압축 파일을 사용자에게 전달
3. 사용자는 압축 해제 후 `SandRaiders.exe` 실행

---

## 🎯 권장 사항

### 일반 사용자 배포
→ **단일 EXE 방식** 사용
- 가장 간단하고 실수할 여지가 없음
- 파일 하나만 전달하면 됨

### 고급 사용자 / 스팀 등
→ **폴더 방식** 사용
- 실행 속도가 빠름
- 자동 설치 스크립트 작성 가능

---

## ⚙️ 빌드 설정 (SandRaiders.spec)

현재 설정:
- **모드**: 단일 EXE (--onefile)
- **콘솔**: 숨김 (console=False)
- **압축**: UPX 사용
- **포함 리소스**: 
  - SDL2 DLL 파일들
  - 게임 리소스 (이미지, 사운드, 폰트)
  - Python 라이브러리

---

## 🐛 문제 해결

### "빌드는 되는데 실행이 안 돼요"
- Windows Defender 예외 처리 필요
- 백신 프로그램에서 차단될 수 있음

### "리소스를 찾을 수 없다는 오류"
- `resource_path.py`가 제대로 적용되었는지 확인
- 모든 `load_image()`, `load_wav()`에 `resource_path()` 적용 확인

### "빌드 시간이 너무 오래 걸려요"
- 정상입니다. 단일 EXE는 5~10분 소요
- 폴더 방식은 2~3분 소요

### "파일 크기가 너무 커요"
- 단일 EXE: 200~300MB (정상)
- 폴더 방식: 전체 150~250MB (정상)
- 리소스 파일(이미지/사운드)이 대부분

---

## 📊 빌드 비교표

| 항목 | 단일 EXE | 폴더 방식 |
|------|----------|----------|
| 배포 편의성 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 실행 속도 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 파일 크기 | 약 250MB | 약 200MB |
| 첫 실행 시간 | 1~3초 | 즉시 |
| 사용자 실수 가능성 | 없음 | 있음 (파일 분리) |
| 개발/디버깅 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

---

## 💡 팁

1. **개발 중**: 폴더 방식 사용 (빠른 빌드, 쉬운 디버깅)
2. **최종 배포**: 단일 EXE 사용 (사용자 편의성)
3. **양쪽 다 빌드**: 두 가지 방식 모두 제공 가능

---

## 📝 체크리스트

빌드 전 확인사항:
- [ ] 모든 리소스 파일 경로가 `resource_path()` 사용
- [ ] 게임이 소스 코드에서 정상 실행됨
- [ ] PyInstaller 설치됨
- [ ] SDL2 DLL 경로 확인

배포 전 확인사항:
- [ ] 빌드된 EXE가 정상 실행됨
- [ ] 모든 리소스(이미지/사운드)가 제대로 로드됨
- [ ] 게임 기능이 모두 작동함
- [ ] README.md 파일 포함

---

**제작**: 2DGP Game Project  
**엔진**: Pico2D + PyInstaller  
**빌드 도구**: PyInstaller 6.17.0

